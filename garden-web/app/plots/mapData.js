const widthMultiplier = 1;  // Adjust as needed
const heightMultiplier = 1.0; // Adjust as needed
const leftMultiplier = 1;   // Adjust as needed
const topMultiplier = 1.0;    // Adjust as needed

const mapData = [
    {
        "width": `${12.924424972617743 * widthMultiplier}%`,
        "height": `${2.2906227630637077 * heightMultiplier}%`,
        "left": `${49.173398138006576 * leftMultiplier}%`,
        "top": `${81.10236220472441 * topMultiplier}%`,
        style: { cursor: 'pointer', },
    },
    {
        "width": `${12.924424972617743 * widthMultiplier}%`,
        "height": `${2.2906227630637077 * heightMultiplier}%`,
        "left": `${49.940101314348304 * leftMultiplier}%`,
        "top": `${77.80959198282032 * topMultiplier}%`,
        style: { background: 'rgba(255, 0, 0, 0.5)' }
    }
];
 
export default mapData;
